<form method="post" action="{{ path('missions_delete', {'alias': mission.alias}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission.alias) }}">
    <button class="btn">Delete</button>
</form>
