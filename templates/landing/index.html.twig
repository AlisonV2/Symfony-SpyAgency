{% extends 'base.html.twig' %}

{% block title %}SpyAgency{% endblock %}

{% block body %}
   <!-- Navbar start -->
    <nav class="navbar navbar-expand">
        <div class="container-fluid">
            <h1 class="navbar-brand">SpyAgency</h1>
            <ul class="navbar-nav">
                <li class="nav-item px-3"><a href="{{ path('app_login') }}">Log in<i class="fas fa-sign-in-alt"></i></a></li>
            </ul>
        </div>
    </nav>
    <!-- Navbar end -->
    <section>
        <div class="container" id="landing">
            <div class="row align-items-center" id="welcome">
                <div class="col-md-12 text-align-center">
                    <p class="landing">Welcome to the SpyAgency</p>
                    <p class="questions">Tell me... </p>
                </div>
            </div>
            <div class="row align-items-center">
                <div class="col-md-6 text-align-center">
                    <a class="questions" href="{{ path('home_index') }}">Are you a visitor?</a>
                </div>
                <div class="col-md-6 text-align-center">
                    <a class="questions" href="{{ path('app_login') }}">Or an admin?</a>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
